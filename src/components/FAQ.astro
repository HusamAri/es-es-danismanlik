---
interface QA {
  q: string;
  a: string;
}

interface Props {
  items: QA[];
  startOpen?: number;
}

const { items, startOpen = 0 } = Astro.props;
---

<div class="mx-auto max-w-3xl">
  {
    items.map((item, idx) => (
      <details
        class="group border-b border-[var(--border-whisper)] py-6 [&_summary::-webkit-details-marker]:hidden last:border-b-0"
        open={idx === startOpen}
      >
        <summary class="flex cursor-pointer items-start justify-between gap-6 text-left">
          <span class="text-base text-navy-800" style="font-family: var(--font-display); font-weight: 500; letter-spacing: -0.01em;">{item.q}</span>
          <span
            aria-hidden="true"
            class="mt-1 grid h-7 w-7 shrink-0 place-items-center border border-[var(--border-soft)] text-gold-500 transition-transform group-open:rotate-45"
          >
            <svg width="12" height="12" viewBox="0 0 14 14" fill="none">
              <path d="M7 1.5v11M1.5 7h11" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" />
            </svg>
          </span>
        </summary>
        <div class="mt-4 pr-12 text-sm text-ink-secondary" style="line-height: 1.75;">
          <p>{item.a}</p>
        </div>
      </details>
    ))
  }
</div>
